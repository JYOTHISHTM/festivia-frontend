import VerifyOtp from "../../components/user/VerifyOtp"; // Import the UI component

const Verifyotp = () => {

  return (
    <div>
      <VerifyOtp /> 
    </div>
  );
};

export default Verifyotp;
